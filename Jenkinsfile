pipeline {

    agent any

    stages {

        stage("build") {
            echo "Building..."
        }

        stage("test") {
            echo "Testing..."
        }

        stage("analyse") {
            echo "Analysing..."
        }

        stage("deploy") {
            echo "Deploying..."
        }
    }
}