pipeline {

    agent any

    stages {
        stage("build") {
            steps {
                mvn clean install
            }
        }

        stage("test") {
            steps {
                echo "Building..."
            }
        }

        stage("analyse") {
            steps {
                echo "Building..."
            }
        }

        stage("deploy") {
            steps {
                echo "Building..."
            }
        }
    }
}